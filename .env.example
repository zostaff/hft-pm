# Polymarket V2 credentials. See CLAUDE.md "Critical Rules" #10 and docs §7.5.
# Copy to .env (gitignored) and fill in for live trading. Phase 1 does not
# read any of these; they are committed here so the contract is explicit.

PRIVATE_KEY=
DEPOSIT_WALLET_ADDRESS=
CLOB_API_KEY=
CLOB_SECRET=
CLOB_PASS_PHRASE=
